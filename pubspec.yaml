name: harikrushna_group_rajkot
description: Hotel Booking App for Harikrushna Group.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.1.1
  cloud_firestore: ^5.0.2    # ફક્ત આ વર્ઝન બદલવાનું છે
  http: ^1.2.1
  google_fonts: ^5.1.0
  intl: ^0.19.0
  table_calendar: ^3.1.2
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
